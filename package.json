{
  "name": "financial-news-hub",
  "version": "1.0.0",
  "description": "A modern financial news aggregation platform",
  "scripts": {
    "dev": "concurrently \"cd backend && pnpm dev\" \"cd frontend && pnpm dev\"",
    "build": "cd backend && pnpm build && cd ../frontend && pnpm build",
    "start": "cd backend && pnpm start",
    "install:all": "pnpm install && cd backend && pnpm install && cd ../frontend && pnpm install",
    "db:setup": "cd backend && pnpm db:generate && pnpm db:push"
  },
  "keywords": ["news", "finance", "typescript", "react", "nodejs", "mysql"],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}